import 'package:flutter/material.dart';

class RatingNumberGraphSection extends StatelessWidget {
  const RatingNumberGraphSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
